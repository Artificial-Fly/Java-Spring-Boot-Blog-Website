# Spring Blog Application - Учебный проект

## 📝 Описание проекта

Этот проект представляет собой простое блоговое веб-приложение, созданное для изучения:

- **Spring Boot** - основного фреймворка
- **Spring MVC** - архитектурного паттерна Model-View-Controller
- **MySQL** - реляционной базы данных
- **Spring Data JPA** - для работы с данными
- **Thymeleaf** - шаблонизатора для представлений
- **Docker** - контейнеризации приложения

## 🚀 Технологии

- **Backend:** Spring Boot 3.2.5, Java 17  
- **Database:** MySQL 8.0  
- **Frontend:** Thymeleaf, Bootstrap 5.3  
- **Build Tool:** Maven 3.9.12  
- **Containerization:** Docker Compose  
- **ORM:** Spring Data JPA

## 📁 Структура проекта

### Основные пакеты:  
```
com.tutorialproject.website/
├── controllers/     # Контроллеры Spring MVC
│   ├── BlogController.java
│   └── MainController.java
├── models/          # Сущности (Entity)
│   └── Post.java
├── repos/           # Репозитории Spring Data JPA
│   └── PostRepository.java
└── WebsiteApplication.java # Точка входа
```

### Ресурсы:  
```
resources/
├── static/          # Статические файлы (CSS, JS)
├── templates/       # Thymeleaf шаблоны
│   ├── blocks/      # Общие блоки (header, footer)
│   ├── about.html
│   ├── blog-add.html
│   ├── blog-details.html
│   ├── blog-edit.html
│   ├── blog-main.html
│   └── home.html
└── application.properties # Конфигурация приложения
```

## 🛠 Функциональность

### Основные возможности:
- **CRUD операции для блоговых постов:**
  - Создание новых постов
  - Просмотр всех постов
  - Редактирование существующих постов
  - Удаление постов

- **Детали поста:**
  - Просмотр полного текста
  - Счетчик просмотров
  - Редактирование/удаление

- **Навигация:**
  - Главная страница
  - Страница "О нас"
  - Блог со списком всех постов
  - Форма добавления нового поста

## 🐳 Запуск приложения

### Способ 1: С использованием Docker Compose (рекомендуется)
1. Убедитесь, что Docker и Docker Compose установлены
2. В корне проекта выполните:
   ```bash
   docker-compose up --build
   ```
3. Приложение будет доступно по адресу: **http://localhost:8080**
4. База данных: **http://localhost:3307**

### Способ 2: Локальный запуск (без Docker)
1. Установите MySQL и создайте базу данных:
   ```sql
   CREATE DATABASE spring_website_database;
   CREATE USER 'bloguser'@'localhost' IDENTIFIED BY 'blogpass';
   GRANT ALL PRIVILEGES ON spring_website_database.* TO 'bloguser'@'localhost';
   ```

2. Обновите `application.properties`:
   ```properties
   spring.datasource.url=jdbc:mysql://localhost:3306/spring_website_database
   ```

3. Соберите и запустите приложение:
   ```bash
   ./mvnw spring-boot:run
   ```

## 📊 REST API

### Основные эндпоинты:
- `GET /` - Главная страница
- `GET /about` - Страница "О нас"
- `GET /blog` - Список всех постов
- `GET /blog/add` - Форма добавления поста
- `POST /blog/add` - Создание нового поста
- `GET /blog/{id}` - Детали поста
- `GET /blog/{id}/edit` - Форма редактирования
- `POST /blog/{id}/edit` - Обновление поста
- `POST /blog/{id}/remove` - Удаление поста

## 🔧 Конфигурация

### Основные настройки в `application.properties`:
```properties
server.port=8080
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://mysql-db:3307/spring_website_database
spring.datasource.username=bloguser
spring.datasource.password=blogpass
spring.jpa.show-sql=true
```

### Конфигурация Docker Compose (`docker-compose.yml`):
- Приложение Spring Boot на порту **8080**
- MySQL 8.0 на порту **3307**
- Общая сеть `s-network`
- Постоянное хранилище для данных MySQL
```
